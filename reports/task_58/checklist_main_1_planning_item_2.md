Update the manually-defined TOML overlay types in `zbobr-api/src/config/mod.rs` that are NOT generated by the `config_struct` macro.

**What:** Change `Option<T>` fields to `TomlOption<T>` for leaf fields in:
- `RoleDefinition` (line ~31)
- `ProviderDefinition` (line ~67)
- `StageDefinition` (line ~200)
- `WorkflowToml` (line ~422)

**Merge logic:** Update their `MergeToml` impls to use `.merge()` instead of `.or()` for `TomlOption` fields.

**resolve_paths:** Update `resolve_paths()` methods — these currently match on `Option<T>` fields. Adapt to work with `TomlOption<T>` (e.g., match on `TomlOption::Value(v)` instead of `Some(v)`).

**Conversion to runtime types:** Where these types are converted to final runtime structs (using `Option<T>`), use `into_option()` or `as_option()`.

**Why:** These types bypass the macro and implement merge manually, so they need manual updates to match the new pattern.

**Key constraint:** Only change leaf fields. Nested struct fields like `Option<NestedToml>` remain unchanged. Follow the same pattern the macro uses for consistency.