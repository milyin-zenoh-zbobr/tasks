These Rust files still use the old `error_out: JObjectArray` + `set_error_string` pattern (which is now a missing function). They need to be updated to return `jstring` (null=success) + typed `out` params, using `make_error_jstring`. Also restore any removed doc comments. Files: zenoh-jni/src/zbytes.rs, zenoh-jni/src/zbytes_kotlin.rs, zenoh-jni/src/scouting.rs, zenoh-jni/src/logger.rs, zenoh-jni/src/zenoh_id.rs