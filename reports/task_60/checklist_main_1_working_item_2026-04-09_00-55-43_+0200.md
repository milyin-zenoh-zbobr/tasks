In src/info.rs, the function `zc_internal_transport_from_fields` currently hardcodes `false` for the `is_shm` argument when calling `Transport::new_from_fields`. The user's requirement is to conditionally expose `is_shm: bool` as a function parameter when the `shared-memory` feature is enabled (using `#[cfg(feature = "shared-memory")]` on the parameter). After fixing, rebuild with ZENOHC_BUILD_WITH_SHARED_MEMORY=ON and ZENOHC_BUILD_WITH_UNSTABLE_API=ON to regenerate include/zenoh_commons.h.