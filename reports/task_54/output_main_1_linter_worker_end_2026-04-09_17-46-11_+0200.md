✅ **Formatting fixed!** 

Fixed the import formatting issue in `examples/z_info/z_info.go`. The file now passes the `make fmt` check with no errors. Go's formatter reorganized the imports into the correct grouping and ordering.